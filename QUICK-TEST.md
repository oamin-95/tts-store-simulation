# 🚀 اختبار سريع للتكامل

## خطوات الاختبار السريعة

### 1. التحقق من Keycloak
```bash
curl -s http://localhost:8090 | head -5
```
✅ يجب أن ترى صفحة HTML من Keycloak

### 2. تشغيل Queue Worker
```bash
cd ~/saas-marketplace
php artisan queue:work &
```

### 3. تشغيل المتجر
```bash
php artisan serve --port=4000
```

### 4. التسجيل في المتجر
افتح في المتصفح:
```
http://localhost:4000/register
```

سجل مستخدم جديد بالبيانات التالية:
- الاسم: اختبار
- الشركة: شركة اختبار
- Email: test@example.com
- Password: password123

### 5. التحقق من Dashboard
بعد التسجيل مباشرة:
1. ستجد رسالة ترحيب
2. ثلاث كروت للمنتجات
3. **كرت جديد بخلفية بنفسجية لـ Keycloak** 🎉

### 6. فتح لوحة Keycloak
في كرت Keycloak:
- اضغط "افتح لوحة الإدارة"
- سيفتح: `http://localhost:8090/admin/tenant-1/console`
- سجل دخول بالبيانات المعروضة
- غير كلمة المرور المؤقتة
- ستصل للوحة Keycloak الخاصة بك! ✅

### 7. التحقق من Realm
```bash
# عرض معلومات Realms
cd ~/saas-marketplace
php test-keycloak-isolation.php
```

### 8. اختبار API
```bash
# اختبار مزامنة الأدوار
curl -X POST http://localhost:4000/api/keycloak/sync-roles \
  -H "Content-Type: application/json" \
  -d '{
    "subscription_id": 1,
    "product": "training",
    "roles": [
      {"name": "admin", "description": "مدير"},
      {"name": "user", "description": "مستخدم"}
    ]
  }'
```

## ✅ النتيجة المتوقعة

بعد هذه الخطوات:
- ✅ تم إنشاء Realm: `tenant-1`
- ✅ صفحة دخول معزولة موجودة
- ✅ لوحة إدارة معزولة موجودة
- ✅ روابط ظاهرة في Dashboard
- ✅ API يعمل بنجاح

## 🎉 مبروك! التكامل يعمل بنجاح
